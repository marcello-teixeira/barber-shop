version: '3.8'

services:
  client:
    image: marcelloteixeira/barbershop-client:2.0
    container_name: client-container
    ports:
      - "80:80"
    depends_on:
      - api
      - sqlserver
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest 
    container_name: sqlserver-container
    environment:
      - SA_PASSWORD
      - ACCEPT_EULA
    ports:
      - "1433:1433"
    volumes:
      - database-data:/var/opt/mssql
    networks:
      - barbershop-backend
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-container
    ports: 
      - "8080:8080"
    depends_on:
      - sqlserver
    environment:
      - ConnectionStrings__SQLServerConnection
      - API_KEY
    networks:
      - barbershop-backend
volumes:
  database-data:    
networks:
  barbershop-backend:
    driver: bridge



    